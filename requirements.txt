numpy>=1.24
pandas>=2.0
scikit-learn>=1.3

torch>=2.2
torchvision>=0.17
torchaudio>=2.2
torchmetrics>=1.3
lightning>=2.2

transformers>=4.40
tensorboard>=2.14

# Needed for optional parquet IO in manifest loaders.
pyarrow>=14.0

# Needed by subset_data.py when using --download.
yt-dlp>=2024.3.10

# Note: ffmpeg is required by subset_data.py but must be installed as a system binary.
